<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= task.id %> : <%= task.status %> &gt; <%= task.content %> /<%= link_to 'このタスクの詳細を開く', task %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの追加', new_task_path %>